fun to_percents(v: i32, a: i32, b: i32): i32 {
    return (v - a) * 100 / (b - a);
}

fun main() {
    const a: i32 = 1;
    const b: i32 = 2;
    println(to_percents(3, 0, 10));
    println("asdasdasd");
    return;
}